@model IEnumerable<VirtualPlay.Business.Models.Pay_RateItems>

@{
    ViewBag.Title = "Taxa";
}

<h2>Taxa</h2>

<p>
    @Html.ActionLink("Novo Item", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Código
        </th>
        <th>
            Nome/Descrição
        </th>
        <th>
            Valor
        </th>
        <th>
            Dias
        </th>
        <th>
            Status
        </th>
        <th>
            Criado em
        </th>
        <th>
            Atualizado em
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pay_Rate.cdRate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nmRateItem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nbDays)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.flStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dtCreate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dtLastUpdate)
            </td>
        <td>
            @Html.ActionLink("Alterar", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Detalhes", "Details", new { /* id=item.PrimaryKey */ })
            @* |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
        </td>
    </tr>
}

</table>
